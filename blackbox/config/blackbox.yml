modules:
  http_2xx:
    prober: http
    timeout: 10s
    http:
      valid_http_versions: ["HTTP/1.1", "HTTP/2"]
      valid_status_codes: [200, 201, 204]
      method: GET
      fail_if_ssl_certificate_expires_in: 2592000  # 30 days
      no_follow_redirects: false
